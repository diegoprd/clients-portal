'use strict';

angular.module('cp-app').controller('ClientFormCtrl', ['$scope', '$localStorage', '$location', function($scope, $localStorage, $location) {

  var initialize = function() {
  };

  initialize();

}]);